srcObj notes;
 TO DO:
1. Mean Flow MMS
    a.  get error calculation on the mean flow in and out to ensure that 
the mean flow that is used as an input is actually being used as intended and 
that the Akima spline interpolation worked as intended. 
    b.  get error calculation on the mean flow's speed of sound numerical 
integration technique.

Both a. and b. require error and then L2 calculations. b. requires an
alpha (asymptotic rate of convergence) plot. Then we c
        
2. Acoustic MMS

V-Put analytical source terms in a module
 -redefine the MMS to account for liner boundary condition
 -pass the input flow variables to both the swirlClassObject and the new source term generation moduls
V-set up loop so that the grid points double each time

