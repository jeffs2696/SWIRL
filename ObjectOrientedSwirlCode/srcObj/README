srcObj notes;
 TO DO:
 -Put analytical source terms in a module
 -redefine the MMS
 -pass the input flow variables to both the swirlClassObject and the new source term generation modules
 -set up loop so that the grid points double each time

